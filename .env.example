PORT=8787
SETTINGS_PATH=/data/settings.json

# Access passwords. Change before deploying anywhere public.
USER_PASSWORD=change-me-user
ADMIN_PASSWORD=change-me-admin
# Secret used to sign scope tokens. Change in production.
AUTH_SECRET=change-me-auth-secret

# HyperFrames producer render concurrency.
HF_RENDER_WORKERS=1
